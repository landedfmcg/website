# Netlify configuration for the Landed static site.
# The repo root is served as-is; the build step only regenerates sitemap.xml
# (from the .html files on disk) and pings IndexNow. See build.js.

[build]
  publish = "."
  command = "node build.js"
